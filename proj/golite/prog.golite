package main;

import "fmt";

type Point struct {
  x int;
  y int;
};

var c bool;

func main() {

	var a int;
	var b bool;
	var p *Point;
	c = true;
	b = c;
	a = 5;
	a = 3 * 4;
	b = a > 10;
	if (b == true) {
		a = a - (a - 9);
		b = false;
		fmt.Print(a);
	} else {
		a = a / 3;
		fmt.Print(a);
	}
	a = a / 10;
	b = a >= 0 && true || a < 10;
	fmt.Println(a);
	a = 0;
	for(a < 10) {
		a = a + 1;
	}
	fmt.Println(a);
	p = new(Point);
	delete(p);
	return;
}